local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Red = require(ReplicatedStorage.Packages.Red)

return Red.Function("Spin", function()
    return nil
end, function(Reward)
    return Reward
end)